"use strict";
//Write a programme that can take the area of a rectangle.
console.log("(1)");
{
    var length = 15;
    var width = 10;
    var area = length * width;
    console.log(area);
}
//WRITE A PROGRAMME THAT TAKES INPUT AND CALCULATES THE VOLUME OF A CUDE
console.log("(2)");
{
    var length2 = 15;
    var width2 = 17;
    var hieght = 18;
    var volume = length2 * width2 * hieght;
    console.log(volume);
}
//Write a programme that checks if a given number is positive negative or zero
console.log("(3)");
{
    var Unknown = 10;
    if (Unknown < 0)
        console.log("negative");
    if (Unknown > 0)
        console.log("positive");
    else if (Unknown = 0)
        console.log("zero");
}
